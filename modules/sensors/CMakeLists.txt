#Sensors

include_directories(${CMAKE_CURRENT_SOURCE_DIR})
set(PROJ_HDRS ${PROJ_HDRS} ${CMAKE_CURRENT_SOURCE_DIR} )

if (";${TARGET_MODULES};" MATCHES ";HOTSWAP_SENSOR;")
  set(PROJ_SRCS ${PROJ_SRCS} hotswap.c PARENT_SCOPE)
endif()

if (";${TARGET_MODULES};" MATCHES ";TEMPERATURE_SENSOR;")
  set(PROJ_SRCS ${PROJ_SRCS} lm75.c PARENT_SCOPE)
endif()

if (";${TARGET_MODULES};" MATCHES ";VOLTAGE_SENSOR;")
  set(PROJ_SRCS ${PROJ_SRCS} ina220.c PARENT_SCOPE)
endif()

