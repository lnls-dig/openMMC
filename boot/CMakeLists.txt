set(BOOT_PATH ${CMAKE_CURRENT_SOURCE_DIR})

set(BOOT_SRCS ${BOOT_SRCS}
  ${UCONTROLLER_SRCS}
  ${BOOT_PATH}/boot.c )

set(BOOT_SRCS ${BOOT_SRCS} PARENT_SCOPE)

set(PROJ_HDRS ${PROJ_HDRS} ${BOOT_PATH} PARENT_SCOPE)
